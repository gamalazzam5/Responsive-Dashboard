class ExpensesItemModel {
  final String image, text, date, amount;

  const ExpensesItemModel({
    required this.image,
    required this.text,
    required this.date,
    required this.amount,
  });
}
